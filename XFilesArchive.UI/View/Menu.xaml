<UserControl x:Class="XFilesArchive.UI.View.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XFilesArchive.UI.View"
             xmlns:viewModel="clr-namespace:XFilesArchive.UI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Menu FontSize="10" >
            <MenuItem Header="Create">
                <MenuItem Header="New Drive" Command="{Binding CreateNewCommand}"
                          CommandParameter="{x:Type viewModel:DriveDetailViewModel}"/>
                <!--<MenuItem Header="New Category" Command="{Binding CreateNewCommand}"
                          CommandParameter="{x:Type viewModel:MeetingDetailViewModel}"/>-->

            </MenuItem>
            <MenuItem Header="Edit">
                <!--<MenuItem Header="Prog Leng" Command="{Binding OpenSingleDetailViewCommand}"
                          CommandParameter="{x:Type viewModel:ProgrammingLanguageDetailViewModel}"/>-->
            </MenuItem>
        </Menu>
    </Grid>
</UserControl>
