<Window x:Class="XFilesArchive.UI.View.Services.MultySelectEntityesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XFilesArchive.UI.View.Services"
        mc:Ignorable="d"
        Title="Множественный выбор" Height="600" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Модель" Binding="{Binding Path=Title}" Width="90" />
                <DataGridHyperlinkColumn Header="Компания" Binding="{Binding Path=EntityPath}" Width="80" />
                
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1">
            <Button Content="OK"/>
            <Button Content="Cancel"/>
        </StackPanel>
    </Grid>
</Window>
