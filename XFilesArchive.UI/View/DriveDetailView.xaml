<UserControl x:Class="XFilesArchive.UI.View.DriveDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:XFilesArchive.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Label Content="Title"/>
        <TextBox Grid.Row="1" Text="{Binding Drive.Title,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
        <view:FilesOnDriveView DataContext="{Binding FilesOnDriveViewModel}" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,10,0,0"/>
        <Button Content="Save" Command="{Binding SaveCommand}" Width="125" Margin="10"/>

    </Grid>

</UserControl>
